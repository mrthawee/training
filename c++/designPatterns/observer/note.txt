Observer Pattern

The Observer Pattern defines a one-to-many dependency between objects so that 
when one object chances state, all of its dependents are notified and updated
automatically

Keys:
1. one-to-many relationship
2. relationship can be
   - register
   - remove
   - notify
3. update() sends info to all
