/*
 * multiset:
 * 1. Fast search: O(log(n))
 * 2. Traversing is slow  (compared to vector & deque)
 * 3. No random access, no [] operator
 */

#include <iostream>
#include <set>

using namespace std;

int main(void) {
   multiset<int> myset;

   return 0;
}
